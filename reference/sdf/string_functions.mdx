---
title: "String Functions"
---

## ascii
```sql
function ascii(x varchar) returns +i32
```
Returns the ascii value of the first character given

```sql example.sql
select ascii('b') as value; -- value '98' 
```
```sql example.sql
select ascii('ab') as value -- value '97' 
```
## bit_length
```sql
function bit_length(x varchar) returns +i32
```
Returns the bit length of the given characters

```sql example.sql
select bit_length('b') as value; -- value '8' 
```
```sql example.sql
select bit_length('hello world') as value -- value '88' 
```
## btrim
```sql
function btrim(x varchar, y varchar) returns varchar
```
Trims y from x if x begins with y.

```sql example.sql
select btrim('hello world', 'hello') as value; -- value ' world' 
```
```sql example.sql
select btrim('hellohello world', 'hello') as value; -- value 'hello world' 
```
```sql example.sql
select btrim('hi hello world', 'hello') as value; -- value 'hi hello world' 
```
## char_length
```sql
function char_length(n varchar) returns +i32
```
Returns the number of characters of n

```sql example.sql
select char_length('hello world!') as value; -- value '12' 
```
## character_length
```sql
function character_length(n varchar) returns +i32
```
Returns the number of characters of n

```sql example.sql
select character_length('hello world!') as value; -- value '12' 
```
## chr
```sql
function chr(+i64) returns varchar
```
Returns the Unicode code point n as a single character string.

```sql example.sql
select chr(61) as value; -- value '=' 
```
```sql example.sql
select chr(128521) as value; -- value '😉' 
```
## concat
```sql
function concat($1, +list<$1>) returns +list<$1>
```
Concatenates the arrays array1, array2, ..., arrayN. This function provides the
same functionality as the SQL-standard concatenation operator (||).

```sql example.sql
select concat('hello ', 'world!') as value; -- value 'hello world!' 
```
```sql example.sql
select concat(['hello '], ['world!']) as value -- value '[hello ][world!]' 
```
## concat_ws
```sql
function concat_ws(varchar, +list<varchar>) returns varchar
```
Returns the concatenation of string1, string2, ..., stringN using string0 as a
separator. If string0 is null, then the return value is null. Any values
provided in the arguments after the separator are skipped.

```sql example.sql
select concat_ws('|', 'a', 'b') as value; -- value 'a|b' 
```
```sql example.sql
select concat_ws('|', 'a', NULL, 'b') as value; -- value 'a|b' 
```
```sql example.sql
select concat_ws(NULL, 'a', 'b') as value; -- value 'NULL' 
```
## digest
```sql
function digest(string varchar, algo varchar) returns 
```
Processes string with algo. Currently supported digest algorithms are md5,
sha224, sha256, sha384, sha512, blake2s, blake2b, blake3

```sql example.sql
select digest('world', 'md5') as value; -- value '7d793037a0760186574b0282f2f435e7' 
```
## initcap
```sql
function initcap(n varchar) returns varchar
```
Capitalizes the first charcter of the provided string. Lower-cases all
subsequent characters.

```sql example.sql
select initcap('a') as value; -- value 'A' 
```
```sql example.sql
select initcap('aAa') as value; -- value 'Aaa' 
```
## left
```sql
function left(n varchar, x +i64) returns varchar
```
Returns the left x characters of n

```sql example.sql
select left('Hello World!', 5) as value; -- value 'Hello' 
```
## length
```sql
function length(varchar) returns +i64
```
Returns the number of characters in n

```sql example.sql
select length('Hello World!') as value; -- value '12' 
```
```sql example.sql
select length('Hey there 😉') as value; -- value '11' 
```
## lower
```sql
function lower(varchar) returns varchar
```
Converts string n to lowercase

```sql example.sql
select lower('Hello World!') as value; -- value 'hello world!' 
```
## lpad
```sql
function lpad(+binary, +i64, +binary) returns +binary
```
Left pads string to size characters with padstring. If size is less than the
length of string, the result is truncated to size characters. size must not be
negative and padstring must be non-empty.

```sql example.sql
select lpad('😉', 5, '*') as value; -- value '****😉' 
```
```sql example.sql
select lpad('world', 15, 'hello') as value; -- value 'hellohelloworld' 
```
## ltrim
```sql
function ltrim(varchar) returns varchar
```
Removes all leading whitespace from string.

```sql example.sql
select ltrim('   😉') as value; -- value '😉' 
```
## md5
```sql
function md5(+binary) returns +binary
```
Computes the md5 hash of string. Md5 is a consistent hash.

```sql example.sql
select md5('Hello World!') as value; -- value 'ed076287532e86365e841e92bfc50d8c' 
```
## octet_length
```sql
function octet_length(string varchar) returns +i32
```
Returns the number of bytes of string

```sql example.sql
select octet_length('Hello World!') as value; -- value '12' 
```
```sql example.sql
select octet_length('😉') as value; -- value '4' 
```
## repeat
```sql
function repeat($1, +i64) returns +list<$1>
```
Repeats string n times

```sql example.sql
select repeat('😉', 4) as value; -- value '😉😉😉😉' 
```
## replace
```sql
function replace(varchar, varchar, varchar) returns varchar
```
Removes all instances of search from string.

```sql example.sql
select replace('Hello There', 'There') as value; -- value 'Hello ' 
```
```sql
function replace(varchar, varchar, varchar) returns varchar
```
Removes all instances of search from string.

```sql example.sql
select replace('Hello There', 'There') as value; -- value 'Hello ' 
```
## reverse
```sql
function reverse(varchar) returns varchar
```
Returns string in reverse order

```sql example.sql
select reverse('Hello There') as value; -- value 'ereht olleH' 
```
## right
```sql
function right(n varchar, x +i64) returns varchar
```
Returns the right x characters of n

```sql example.sql
select right('Hello World!', 6) as value; -- value 'World!' 
```
## rpad
```sql
function rpad(+binary, +i64, +binary) returns +binary
```
Right pads string to size characters with padstring. If size is less than the
length of string, the result is truncated to size characters. size must not be
negative and padstring must be non-empty.

```sql example.sql
select rpad('😉', 5, '*') as value; -- value '😉****' 
```
```sql example.sql
select rpad('world', 15, 'hello') as value; -- value 'worldhellohello' 
```
## rtrim
```sql
function rtrim(varchar) returns varchar
```
Removes all trailing whitespace from string.

```sql example.sql
select rtrim('😉   ') as value; -- value '😉' 
```
## split_part
```sql
function split_part(varchar, varchar, +i64) returns varchar
```
Splits string on delimiter and returns the field index. Field indexes start with
1. If the index is larger than the number of fields, then null is returned.

```sql example.sql
select split_part('Hello|World', '|', 1) as value; -- value 'Hello' 
```
## starts_with
```sql
function starts_with(varchar, varchar) returns boolean
```
Tests whether substring is a prefix of string

```sql example.sql
select starts_with('Hello|World', 'Hello') as value; -- value 'true' 
```
## strpos
```sql
function strpos(varchar, varchar) returns +i64
```
Returns the starting position of the first instance of substring in string.
Positions start with 1. If not found, 0 is returned.

```sql example.sql
select strpos('Hello World', 'World') as value; -- value '7' 
```
```sql
function strpos(varchar, varchar) returns +i64
```
Returns the starting position of the first instance of substring in string.
Positions start with 1. If not found, 0 is returned.

```sql example.sql
select strpos('Hello World', 'World') as value; -- value '7' 
```
## substr
```sql
function substr(+binary, +i64) returns +binary
```
Returns the rest of string from the starting position start. Positions start
with 1. A negative starting position is interpreted as being relative to the end
of the string.

```sql example.sql
select substr('Hello World', 6) as value; -- value ' World' 
```
## substring
```sql
function substring(varchar, +i64) returns varchar
```
Returns the rest of string from the starting position start. Positions start
with 1. A negative starting position is interpreted as being relative to the end
of the string.

```sql example.sql
select substring('Hello World', 6) as value; -- value ' World' 
```
## translate
```sql
function translate(varchar, varchar, varchar) returns varchar
```
Returns the source string translated by replacing characters found in the from
string with the corresponding characters in the to string. If the from string
contains duplicates, only the first is used. If the source character does not
exist in the from string, the source character will be copied without
translation. If the index of the matching character in the from string is beyond
the length of the to string, the source character will be omitted from the
resulting string.

```sql example.sql
select translate('Hello World', 'Hell', 'N') as value; -- value 'No Word' 
```
## trim
```sql
function trim(varchar) returns varchar
```
Removes leading and trailing whitespace from string.

```sql example.sql
select trim('  😉  ') as value; -- value '😉' 
```
## upper
```sql
function upper(varchar) returns varchar
```
Converts string n to uppercase

```sql example.sql
select upper('Hello World!') as value; -- value 'HELLO WORLD!' 
```

