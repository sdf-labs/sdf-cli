---
title: "Aggregate Functions"
---

## array_agg
```sql
function array_agg(t) returns array(t)
```


## bit_count
```sql
function bit_count(bigint, bigint) returns bigint
```


## count
```sql
function count() returns bigint
```


```sql
function count(t) returns bigint
```


## count_if
```sql
function count_if(boolean) returns bigint
```


## hash_counts
```sql
function hash_counts(varchar) returns map<bigint, smallint>
```


## regexp_count
```sql
function regexp_count(varchar, varchar) returns bigint
```



