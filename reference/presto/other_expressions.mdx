---
title: "Other Expressions"
---

## all_match
```sql
function all_match(array(t), lambda <(t) -> boolean>) returns boolean
```


## any_match
```sql
function any_match(array(t), lambda <(t) -> boolean>) returns boolean
```


## approx_distinct
```sql
function approx_distinct(boolean) returns bigint
```


```sql
function approx_distinct(boolean, double) returns bigint
```


```sql
function approx_distinct(t) returns bigint
```


```sql
function approx_distinct(t, double) returns bigint
```


```sql
function approx_distinct(unknown) returns bigint
```


```sql
function approx_distinct(unknown, double) returns bigint
```


## approx_most_frequent
```sql
function approx_most_frequent(bigint, bigint, bigint) returns map<bigint, bigint>
```


```sql
function approx_most_frequent(bigint, varchar, bigint) returns map<varchar, bigint>
```


## approx_percentile
```sql
function approx_percentile(bigint, array<double>) returns array<bigint>
```


```sql
function approx_percentile(bigint, double, array<double>) returns array<bigint>
```


```sql
function approx_percentile(double, array<double>) returns array<double>
```


```sql
function approx_percentile(double, double, array<double>) returns array<double>
```


```sql
function approx_percentile(float, array<double>) returns array<float>
```


```sql
function approx_percentile(float, double, array<double>) returns array<float>
```


```sql
function approx_percentile(bigint, double) returns bigint
```


```sql
function approx_percentile(bigint, double, double) returns bigint
```


```sql
function approx_percentile(bigint, double, double, double) returns bigint
```


```sql
function approx_percentile(double, double) returns double
```


```sql
function approx_percentile(double, double, double) returns double
```


```sql
function approx_percentile(double, double, double, double) returns double
```


```sql
function approx_percentile(float, double) returns float
```


```sql
function approx_percentile(float, double, double) returns float
```


```sql
function approx_percentile(float, double, double, double) returns float
```


## approx_set
```sql
function approx_set(bigint) returns bytea
```


```sql
function approx_set(double) returns bytea
```


```sql
function approx_set(varchar) returns bytea
```


## arbitrary
```sql
function arbitrary(t) returns t
```


## array_distinct
```sql
function array_distinct(array(e)) returns array(e)
```


## array_except
```sql
function array_except(array(e), array(e)) returns array(e)
```


## array_intersect
```sql
function array_intersect(array(e), array(e)) returns array(e)
```


## array_join
```sql
function array_join(array(t), varchar) returns varchar
```


```sql
function array_join(array(t), varchar, varchar) returns varchar
```


## array_position
```sql
function array_position(array(t), t) returns bigint
```


## array_remove
```sql
function array_remove(array(e), e) returns array(e)
```


## array_sort
```sql
function array_sort(array(e)) returns array(e)
```


```sql
function array_sort(array(t), lambda <(t, t) -> bigint>) returns array(t)
```


## array_union
```sql
function array_union(array(e), array(e)) returns array(e)
```


## arrays_overlap
```sql
function arrays_overlap(array(e), array(e)) returns boolean
```


## bar
```sql
function bar(double, bigint) returns varchar
```


```sql
function bar(double, bigint, varchar, varchar) returns varchar
```


## beta_cdf
```sql
function beta_cdf(double, double, double) returns double
```


## bing_tile
```sql
function bing_tile(bigint, bigint, bigint) returns varchar
```


```sql
function bing_tile(varchar) returns varchar
```


## bing_tile_at
```sql
function bing_tile_at(double, double, bigint) returns varchar
```


## bing_tile_coordinates
```sql
function bing_tile_coordinates(varchar) returns struct<x int, y int>
```


## bing_tile_polygon
```sql
function bing_tile_polygon(varchar) returns varchar
```


## bing_tile_quadkey
```sql
function bing_tile_quadkey(varchar) returns varchar
```


## bing_tile_zoom_level
```sql
function bing_tile_zoom_level(varchar) returns tinyint
```


## bing_tiles_around
```sql
function bing_tiles_around(double, double, bigint) returns array<varchar>
```


```sql
function bing_tiles_around(double, double, bigint, double) returns array<varchar>
```


## bitwise_and
```sql
function bitwise_and(bigint, bigint) returns bigint
```


## bitwise_and_agg
```sql
function bitwise_and_agg(bigint) returns bigint
```


## bitwise_left_shift
```sql
function bitwise_left_shift(bigint, bigint) returns bigint
```


```sql
function bitwise_left_shift(int, bigint) returns int
```


```sql
function bitwise_left_shift(smallint, bigint) returns smallint
```


```sql
function bitwise_left_shift(tinyint, bigint) returns tinyint
```


## bitwise_not
```sql
function bitwise_not(bigint) returns bigint
```


## bitwise_or
```sql
function bitwise_or(bigint, bigint) returns bigint
```


## bitwise_or_agg
```sql
function bitwise_or_agg(bigint) returns bigint
```


## bitwise_right_shift
```sql
function bitwise_right_shift(bigint, bigint) returns bigint
```


```sql
function bitwise_right_shift(int, bigint) returns int
```


```sql
function bitwise_right_shift(smallint, bigint) returns smallint
```


```sql
function bitwise_right_shift(tinyint, bigint) returns tinyint
```


## bitwise_right_shift_arithmetic
```sql
function bitwise_right_shift_arithmetic(bigint, bigint) returns bigint
```


```sql
function bitwise_right_shift_arithmetic(int, bigint) returns int
```


```sql
function bitwise_right_shift_arithmetic(smallint, bigint) returns smallint
```


```sql
function bitwise_right_shift_arithmetic(tinyint, bigint) returns tinyint
```


## bitwise_xor
```sql
function bitwise_xor(bigint, bigint) returns bigint
```


## bool_and
```sql
function bool_and(boolean) returns boolean
```


## bool_or
```sql
function bool_or(boolean) returns boolean
```


## cardinality
```sql
function cardinality(array(e)) returns bigint
```


```sql
function cardinality(bytea) returns bigint
```


```sql
function cardinality(map(k, v)) returns bigint
```


```sql
function cardinality(varchar) returns bigint
```


## cbrt
```sql
function cbrt(double) returns double
```


## ceil
```sql
function ceil(decimal(p, s)) returns decimal(rp, 0)
```


```sql
function ceil(double) returns double
```


```sql
function ceil(float) returns float
```


## ceiling
```sql
function ceiling(decimal(p, s)) returns decimal(rp, 0)
```


```sql
function ceiling(double) returns double
```


```sql
function ceiling(float) returns float
```


## char2hexint
```sql
function char2hexint(varchar) returns varchar
```


## chr
```sql
function chr(bigint) returns varchar
```


## classify
```sql
function classify(map<bigint, double>, varchar) returns varchar
```


## codepoint
```sql
function codepoint(varchar) returns int
```


## color
```sql
function color(double, varchar, varchar) returns varchar
```


```sql
function color(double, double, double, varchar, varchar) returns varchar
```


```sql
function color(varchar) returns varchar
```


## combinations
```sql
function combinations(array(t), bigint) returns array(array(t))
```


## contains
```sql
function contains(array(t), t) returns boolean
```


```sql
function contains(varchar, varchar) returns boolean
```


## contains_sequence
```sql
function contains_sequence(array(t), array(t)) returns boolean
```


## convex_hull_agg
```sql
function convex_hull_agg(varchar) returns varchar
```


## corr
```sql
function corr(double, double) returns double
```


```sql
function corr(float, float) returns float
```


## covar_pop
```sql
function covar_pop(double, double) returns double
```


```sql
function covar_pop(float, float) returns float
```


## covar_samp
```sql
function covar_samp(double, double) returns double
```


```sql
function covar_samp(float, float) returns float
```


## crc32
```sql
function crc32(bytea) returns bigint
```


## cume_dist
```sql
function cume_dist() returns double
```


## current_catalog
```sql
function current_catalog() returns varchar
```


## current_groups
```sql
function current_groups() returns array<varchar>
```


## current_schema
```sql
function current_schema() returns varchar
```


## current_user
```sql
function current_user() returns varchar
```


## day
```sql
function day(date) returns bigint
```


```sql
function day(interval day to second) returns bigint
```


```sql
function day(timestamp(p)) returns bigint
```


```sql
function day(timestamp(p)) returns bigint
```


## day_of_month
```sql
function day_of_month(date) returns bigint
```


```sql
function day_of_month(interval day to second) returns bigint
```


```sql
function day_of_month(timestamp(p)) returns bigint
```


```sql
function day_of_month(timestamp(p)) returns bigint
```


## day_of_week
```sql
function day_of_week(date) returns bigint
```


```sql
function day_of_week(timestamp(p)) returns bigint
```


```sql
function day_of_week(timestamp(p)) returns bigint
```


## day_of_year
```sql
function day_of_year(date) returns bigint
```


```sql
function day_of_year(timestamp(p)) returns bigint
```


```sql
function day_of_year(timestamp(p)) returns bigint
```


## degrees
```sql
function degrees(double) returns double
```


## dense_rank
```sql
function dense_rank() returns bigint
```


## dow
```sql
function dow(date) returns bigint
```


```sql
function dow(timestamp(p)) returns bigint
```


```sql
function dow(timestamp(p)) returns bigint
```


## doy
```sql
function doy(date) returns bigint
```


```sql
function doy(timestamp(p)) returns bigint
```


```sql
function doy(timestamp(p)) returns bigint
```


## e
```sql
function e() returns double
```


## element_at
```sql
function element_at(map(k, v), k) returns v
```


```sql
function element_at(array(e), bigint) returns e
```


## empty_approx_set
```sql
function empty_approx_set() returns bytea
```


## evaluate_classifier_predictions
```sql
function evaluate_classifier_predictions(bigint, bigint) returns varchar
```


```sql
function evaluate_classifier_predictions(varchar, varchar) returns varchar
```


## every
```sql
function every(boolean) returns boolean
```


## exp
```sql
function exp(double) returns double
```


## features
```sql
function features(double) returns map<bigint, double>
```


```sql
function features(double, double) returns map<bigint, double>
```


```sql
function features(double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double, double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double, double, double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double, double, double, double, double, double) returns map<bigint, double>
```


```sql
function features(double, double, double, double, double, double, double, double, double, double) returns map<bigint, double>
```


## filter
```sql
function filter(array(t), lambda <(t) -> boolean>) returns array(t)
```


## first_value
```sql
function first_value(t) returns t
```


## flatten
```sql
function flatten(array(array(e))) returns array(e)
```


## floor
```sql
function floor(decimal(p, s)) returns decimal(rp, 0)
```


```sql
function floor(double) returns double
```


```sql
function floor(float) returns float
```


## format
```sql
function format(T, ...) returns varchar
```


## format_number
```sql
function format_number(bigint) returns varchar
```


```sql
function format_number(double) returns varchar
```


## from_base
```sql
function from_base(varchar, bigint) returns bigint
```


## from_base32
```sql
function from_base32(bytea) returns bytea
```


```sql
function from_base32(varchar) returns bytea
```


## from_base64
```sql
function from_base64(bytea) returns bytea
```


```sql
function from_base64(varchar) returns bytea
```


## from_base64url
```sql
function from_base64url(bytea) returns bytea
```


```sql
function from_base64url(varchar) returns bytea
```


## from_big_endian_32
```sql
function from_big_endian_32(bytea) returns int
```


## from_big_endian_64
```sql
function from_big_endian_64(bytea) returns bigint
```


## from_encoded_polyline
```sql
function from_encoded_polyline(varchar) returns varchar
```


## from_geojson_geometry
```sql
function from_geojson_geometry(varchar) returns varchar
```


## from_hex
```sql
function from_hex(bytea) returns bytea
```


```sql
function from_hex(varchar) returns bytea
```


## from_ieee754_32
```sql
function from_ieee754_32(bytea) returns float
```


## from_ieee754_64
```sql
function from_ieee754_64(bytea) returns double
```


## from_utf8
```sql
function from_utf8(bytea) returns varchar
```


```sql
function from_utf8(bytea, bigint) returns varchar
```


```sql
function from_utf8(bytea, varchar) returns varchar
```


## geometry_from_hadoop_shape
```sql
function geometry_from_hadoop_shape(bytea) returns varchar
```


## geometry_invalid_reason
```sql
function geometry_invalid_reason(varchar) returns varchar
```


## geometry_nearest_points
```sql
function geometry_nearest_points(varchar, varchar) returns struct<c0 varchar, c1 varchar>
```


## geometry_to_bing_tiles
```sql
function geometry_to_bing_tiles(varchar, bigint) returns array<varchar>
```


## geometry_union
```sql
function geometry_union(array<varchar>) returns varchar
```


## geometry_union_agg
```sql
function geometry_union_agg(varchar) returns varchar
```


## greatest
```sql
function greatest(e, ...) returns e
```


## histogram
```sql
function histogram(t) returns map(t, bigint)
```


## hmac_md5
```sql
function hmac_md5(bytea, bytea) returns bytea
```


## hmac_sha1
```sql
function hmac_sha1(bytea, bytea) returns bytea
```


## hmac_sha256
```sql
function hmac_sha256(bytea, bytea) returns bytea
```


## hmac_sha512
```sql
function hmac_sha512(bytea, bytea) returns bytea
```


## hour
```sql
function hour(interval day to second) returns bigint
```


```sql
function hour(time(p)) returns bigint
```


```sql
function hour(time(p)) returns bigint
```


```sql
function hour(timestamp(p)) returns bigint
```


```sql
function hour(timestamp(p)) returns bigint
```


## human_readable_seconds
```sql
function human_readable_seconds(double) returns varchar
```


## if
```sql
function if(boolean, T, T) returns T
```


## index
```sql
function index(varchar, varchar) returns bigint
```


## infinity
```sql
function infinity() returns double
```


## intersection_cardinality
```sql
function intersection_cardinality(varchar, varchar) returns bigint
```


## inverse_beta_cdf
```sql
function inverse_beta_cdf(double, double, double) returns double
```


## inverse_normal_cdf
```sql
function inverse_normal_cdf(double, double, double) returns double
```


## is_finite
```sql
function is_finite(double) returns boolean
```


## is_infinite
```sql
function is_infinite(double) returns boolean
```


## is_json_scalar
```sql
function is_json_scalar(varchar) returns boolean
```


```sql
function is_json_scalar(varchar) returns boolean
```


## is_nan
```sql
function is_nan(double) returns boolean
```


```sql
function is_nan(float) returns boolean
```


## jaccard_index
```sql
function jaccard_index(varchar, varchar) returns double
```


## json_array_contains
```sql
function json_array_contains(varchar, bigint) returns boolean
```


```sql
function json_array_contains(varchar, boolean) returns boolean
```


```sql
function json_array_contains(varchar, double) returns boolean
```


```sql
function json_array_contains(varchar, varchar) returns boolean
```


```sql
function json_array_contains(varchar, bigint) returns boolean
```


```sql
function json_array_contains(varchar, boolean) returns boolean
```


```sql
function json_array_contains(varchar, double) returns boolean
```


```sql
function json_array_contains(varchar, varchar) returns boolean
```


## json_array_get
```sql
function json_array_get(varchar, bigint) returns varchar
```


```sql
function json_array_get(varchar, bigint) returns varchar
```


## json_extract
```sql
function json_extract(varchar, varchar) returns varchar
```


```sql
function json_extract(varchar, varchar) returns varchar
```


## json_extract_scalar
```sql
function json_extract_scalar(varchar, varchar) returns varchar
```


```sql
function json_extract_scalar(varchar, varchar) returns varchar
```


## json_format
```sql
function json_format(varchar) returns varchar
```


## json_parse
```sql
function json_parse(varchar) returns varchar
```


## json_size
```sql
function json_size(varchar, varchar) returns bigint
```


```sql
function json_size(varchar, varchar) returns bigint
```


## kurtosis
```sql
function kurtosis(bigint) returns double
```


```sql
function kurtosis(double) returns double
```


## lag
```sql
function lag(t) returns t
```


```sql
function lag(t, bigint) returns t
```


```sql
function lag(t, bigint, t) returns t
```


## last_day_of_month
```sql
function last_day_of_month(date) returns date
```


```sql
function last_day_of_month(timestamp(p)) returns date
```


```sql
function last_day_of_month(timestamp(p)) returns date
```


## last_value
```sql
function last_value(t) returns t
```


## lead
```sql
function lead(t) returns t
```


```sql
function lead(t, bigint) returns t
```


```sql
function lead(t, bigint, t) returns t
```


## learn_classifier
```sql
function learn_classifier(bigint, map<bigint, double>) returns varchar
```


```sql
function learn_classifier(double, map<bigint, double>) returns varchar
```


```sql
function learn_classifier(varchar, map<bigint, double>) returns varchar
```


## learn_libsvm_classifier
```sql
function learn_libsvm_classifier(bigint, map<bigint, double>, varchar) returns varchar
```


```sql
function learn_libsvm_classifier(double, map<bigint, double>, varchar) returns varchar
```


```sql
function learn_libsvm_classifier(varchar, map<bigint, double>, varchar) returns varchar
```


## learn_libsvm_regressor
```sql
function learn_libsvm_regressor(bigint, map<bigint, double>, varchar) returns varchar
```


```sql
function learn_libsvm_regressor(double, map<bigint, double>, varchar) returns varchar
```


## learn_regressor
```sql
function learn_regressor(bigint, map<bigint, double>) returns varchar
```


```sql
function learn_regressor(double, map<bigint, double>) returns varchar
```


## least
```sql
function least(e, ...) returns e
```


## line_interpolate_point
```sql
function line_interpolate_point(varchar, double) returns varchar
```


## line_interpolate_points
```sql
function line_interpolate_points(varchar, double) returns array<varchar>
```


## line_locate_point
```sql
function line_locate_point(varchar, varchar) returns double
```


## listagg
```sql
function listagg(varchar, varchar, boolean, varchar, boolean) returns varchar
```


## ln
```sql
function ln(double) returns double
```


## log
```sql
function log(double, double) returns double
```


## log10
```sql
function log10(double) returns double
```


## log2
```sql
function log2(double) returns double
```


## lower
```sql
function lower(varchar) returns varchar
```


```sql
function lower(varchar) returns varchar
```


## lpad
```sql
function lpad(bytea, bigint, bytea) returns bytea
```


```sql
function lpad(varchar, bigint, varchar) returns varchar
```


## ltrim
```sql
function ltrim(varchar) returns varchar
```


```sql
function ltrim(varchar, varchar) returns varchar
```


```sql
function ltrim(varchar) returns varchar
```


```sql
function ltrim(varchar, varchar) returns varchar
```


## luhn_check
```sql
function luhn_check(varchar) returns boolean
```


## make_set_digest
```sql
function make_set_digest(t) returns varchar
```


## map
```sql
function map(array(k), array(v)) returns map(k, v)
```


```sql
function map() returns map(unknown, unknown)
```


## map_agg
```sql
function map_agg(k, v) returns map(k, v)
```


## map_entries
```sql
function map_entries(map(k, v)) returns array(row(c0 k, c1 v))
```


## map_filter
```sql
function map_filter(map(k, v), lambda <(k, v) -> boolean>) returns map(k, v)
```


## map_from_entries
```sql
function map_from_entries(array(row(c0 k, c1 v))) returns map(k, v)
```


## map_keys
```sql
function map_keys(map(k, v)) returns array(k)
```


## map_union
```sql
function map_union(map(k, v)) returns map(k, v)
```


## map_values
```sql
function map_values(map(k, v)) returns array(v)
```


## map_zip_with
```sql
function map_zip_with(map(k, v1), map(k, v2), lambda <(k, v1, v2) -> v3>) returns map(k, v3)
```


## md5
```sql
function md5(bytea) returns bytea
```


## merge
```sql
function merge(bytea) returns bytea
```


```sql
function merge(varchar) returns varchar
```


```sql
function merge(varchar) returns varchar
```


## merge_set_digest
```sql
function merge_set_digest(varchar) returns varchar
```


## millisecond
```sql
function millisecond(interval day to second) returns bigint
```


```sql
function millisecond(time(p)) returns bigint
```


```sql
function millisecond(time(p)) returns bigint
```


```sql
function millisecond(timestamp(p)) returns bigint
```


```sql
function millisecond(timestamp(p)) returns bigint
```


## mod
```sql
function mod(bigint, bigint) returns bigint
```


```sql
function mod(decimal(a_precision, a_scale), decimal(b_precision, b_scale)) returns decimal(r_precision, r_scale)
```


```sql
function mod(double, double) returns double
```


```sql
function mod(int, int) returns int
```


```sql
function mod(float, float) returns float
```


```sql
function mod(smallint, smallint) returns smallint
```


```sql
function mod(tinyint, tinyint) returns tinyint
```


## month
```sql
function month(date) returns bigint
```


```sql
function month(interval day to second) returns bigint
```


```sql
function month(timestamp(p)) returns bigint
```


```sql
function month(timestamp(p)) returns bigint
```


## multimap_agg
```sql
function multimap_agg(k, v) returns map(k, array(v))
```


## multimap_from_entries
```sql
function multimap_from_entries(array(row(c0 k, c1 v))) returns map(k, array(v))
```


## murmur3
```sql
function murmur3(bytea) returns bytea
```


## nan
```sql
function nan() returns double
```


## ngrams
```sql
function ngrams(array(t), bigint) returns array(array(t))
```


## none_match
```sql
function none_match(array(t), lambda <(t) -> boolean>) returns boolean
```


## normal_cdf
```sql
function normal_cdf(double, double, double) returns double
```


## normalize
```sql
function normalize(varchar, varchar) returns varchar
```


## now
```sql
function now() returns timestamp(millisecond)
```


## nth_value
```sql
function nth_value(t, bigint) returns t
```


## ntile
```sql
function ntile(bigint) returns bigint
```


## numeric_histogram
```sql
function numeric_histogram(bigint, double) returns map<double, double>
```


```sql
function numeric_histogram(bigint, double, double) returns map<double, double>
```


```sql
function numeric_histogram(bigint, float) returns map<float, float>
```


```sql
function numeric_histogram(bigint, float, double) returns map<float, float>
```


## objectid
```sql
function objectid() returns varchar
```


```sql
function objectid(varchar) returns varchar
```


## parse_data_size
```sql
function parse_data_size(varchar) returns decimal(38, 0)
```


## parse_duration
```sql
function parse_duration(varchar) returns interval day to second
```


## parse_presto_data_size
```sql
function parse_presto_data_size(varchar) returns decimal(38, 0)
```


## percent_rank
```sql
function percent_rank() returns double
```


## pi
```sql
function pi() returns double
```


## pow
```sql
function pow(double, double) returns double
```


## power
```sql
function power(double, double) returns double
```


## qdigest_agg
```sql
function qdigest_agg(bigint) returns varchar
```


```sql
function qdigest_agg(bigint, bigint) returns varchar
```


```sql
function qdigest_agg(bigint, bigint, double) returns varchar
```


```sql
function qdigest_agg(double) returns varchar
```


```sql
function qdigest_agg(double, bigint) returns varchar
```


```sql
function qdigest_agg(double, bigint, double) returns varchar
```


```sql
function qdigest_agg(float) returns varchar
```


```sql
function qdigest_agg(float, bigint) returns varchar
```


```sql
function qdigest_agg(float, bigint, double) returns varchar
```


## quantile_at_value
```sql
function quantile_at_value(varchar, bigint) returns double
```


```sql
function quantile_at_value(varchar, double) returns double
```


```sql
function quantile_at_value(varchar, float) returns double
```


## quarter
```sql
function quarter(date) returns bigint
```


```sql
function quarter(timestamp(p)) returns bigint
```


```sql
function quarter(timestamp(p)) returns bigint
```


## radians
```sql
function radians(double) returns double
```


## rand
```sql
function rand(bigint) returns bigint
```


```sql
function rand(bigint, bigint) returns bigint
```


```sql
function rand() returns double
```


```sql
function rand(int) returns int
```


```sql
function rand(int, int) returns int
```


```sql
function rand(smallint) returns smallint
```


```sql
function rand(smallint, smallint) returns smallint
```


```sql
function rand(tinyint) returns tinyint
```


```sql
function rand(tinyint, tinyint) returns tinyint
```


## random
```sql
function random(bigint) returns bigint
```


```sql
function random(bigint, bigint) returns bigint
```


```sql
function random() returns double
```


```sql
function random(int) returns int
```


```sql
function random(int, int) returns int
```


```sql
function random(smallint) returns smallint
```


```sql
function random(smallint, smallint) returns smallint
```


```sql
function random(tinyint) returns tinyint
```


```sql
function random(tinyint, tinyint) returns tinyint
```


## rank
```sql
function rank() returns bigint
```


## reclassify
```sql
function reclassify(T, varchar, varchar) returns T
```
Changes the classification label of the first argument from the expected classifier specified by the second argument to the desired classifier specified by the third. The expected and desired classifiers must belong to the same classifier group. No other classifiers attached to the first argument are impacted. The function will emit a warning if the the first argument does not have the expected classifier

```sql example.sql
select reclassify(12345, 'pii.clear_text', 'pii.masked') as value; -- value '12345' 
```
```sql
function reclassify(T, varchar) returns T
```
Changes the classification label of the first argument to the desired classifier specified by the second argument. The first argument is expected to have one or more classifiers from the same classifier group as the desired classifier. All of these classifiers will be removed and replaced by the desired classifier. No other classifiers attached to the first argument will be impacted. The function will emit a warning if the first argument does not have a classifier from the expected classifier group

```sql example.sql
select reclassify(12345, 'pii.masked') as value; -- value '12345' 
```
## reduce
```sql
function reduce(array(t), s, lambda <(s, t) -> s>, lambda <(s) -> r>) returns r
```


## reduce_agg
```sql
function reduce_agg(t, s, lambda <(s, t) -> s>, lambda <(s, s) -> s>) returns s
```


## regexp_extract
```sql
function regexp_extract(varchar, varchar) returns varchar
```


```sql
function regexp_extract(varchar, varchar, bigint) returns varchar
```


## regexp_extract_all
```sql
function regexp_extract_all(varchar, varchar) returns array<varchar>
```


```sql
function regexp_extract_all(varchar, varchar, bigint) returns array<varchar>
```


## regexp_like
```sql
function regexp_like(varchar, varchar) returns boolean
```


## regexp_position
```sql
function regexp_position(varchar, varchar) returns int
```


```sql
function regexp_position(varchar, varchar, bigint) returns int
```


```sql
function regexp_position(varchar, varchar, bigint, bigint) returns int
```


## regexp_split
```sql
function regexp_split(varchar, varchar) returns array<varchar>
```


## regr_intercept
```sql
function regr_intercept(double, double) returns double
```


```sql
function regr_intercept(float, float) returns float
```


## regr_slope
```sql
function regr_slope(double, double) returns double
```


```sql
function regr_slope(float, float) returns float
```


## regress
```sql
function regress(map<bigint, double>, varchar) returns double
```


## render
```sql
function render(boolean) returns varchar
```


```sql
function render(bigint, varchar) returns varchar
```


```sql
function render(double, varchar) returns varchar
```


```sql
function render(varchar, varchar) returns varchar
```


## repeat
```sql
function repeat(t, bigint) returns array(t)
```


## reverse
```sql
function reverse(array(e)) returns array(e)
```


```sql
function reverse(bytea) returns bytea
```


```sql
function reverse(varchar) returns varchar
```


## rgb
```sql
function rgb(bigint, bigint, bigint) returns varchar
```


## round
```sql
function round(bigint) returns bigint
```


```sql
function round(bigint, bigint) returns bigint
```


```sql
function round(decimal(p, s)) returns decimal(rp, rs)
```


```sql
function round(decimal(p, s), bigint) returns decimal(rp, s)
```


```sql
function round(double) returns double
```


```sql
function round(double, bigint) returns double
```


```sql
function round(int) returns int
```


```sql
function round(int, bigint) returns int
```


```sql
function round(float) returns float
```


```sql
function round(float, bigint) returns float
```


```sql
function round(smallint) returns smallint
```


```sql
function round(smallint, bigint) returns smallint
```


```sql
function round(tinyint) returns tinyint
```


```sql
function round(tinyint, bigint) returns tinyint
```


## row_number
```sql
function row_number() returns bigint
```


## rpad
```sql
function rpad(bytea, bigint, bytea) returns bytea
```


```sql
function rpad(varchar, bigint, varchar) returns varchar
```


## rtrim
```sql
function rtrim(varchar) returns varchar
```


```sql
function rtrim(varchar, varchar) returns varchar
```


```sql
function rtrim(varchar) returns varchar
```


```sql
function rtrim(varchar, varchar) returns varchar
```


## second
```sql
function second(interval day to second) returns bigint
```


```sql
function second(time(p)) returns bigint
```


```sql
function second(time(p)) returns bigint
```


```sql
function second(timestamp(p)) returns bigint
```


```sql
function second(timestamp(p)) returns bigint
```


## sequence
```sql
function sequence(bigint, bigint) returns array<bigint>
```


```sql
function sequence(bigint, bigint, bigint) returns array<bigint>
```


```sql
function sequence(date, date) returns array[date]
```


```sql
function sequence(date, date, interval day to second) returns array[date]
```


```sql
function sequence(date, date, interval day to second) returns array[date]
```


```sql
function sequence(timestamp(p), timestamp(p), interval day to second) returns array(timestamp(p))
```


```sql
function sequence(timestamp(p), timestamp(p), interval day to second) returns array(timestamp(p))
```


## sha1
```sql
function sha1(bytea) returns bytea
```


## sha256
```sql
function sha256(bytea) returns bytea
```


## sha512
```sql
function sha512(bytea) returns bytea
```


## shuffle
```sql
function shuffle(array(e)) returns array(e)
```


## sign
```sql
function sign(bigint) returns bigint
```


```sql
function sign(decimal(p, s)) returns decimal(1, 0)
```


```sql
function sign(double) returns double
```


```sql
function sign(int) returns int
```


```sql
function sign(float) returns float
```


```sql
function sign(smallint) returns smallint
```


```sql
function sign(tinyint) returns tinyint
```


## simplify_geometry
```sql
function simplify_geometry(varchar, double) returns varchar
```


## skewness
```sql
function skewness(bigint) returns double
```


```sql
function skewness(double) returns double
```


## slice
```sql
function slice(array(e), bigint, bigint) returns array(e)
```


## soundex
```sql
function soundex(varchar) returns varchar
```


## spatial_partitioning
```sql
function spatial_partitioning(varchar) returns varchar
```


## spatial_partitions
```sql
function spatial_partitions(varchar, varchar) returns array<int>
```


```sql
function spatial_partitions(varchar, varchar, double) returns array<int>
```


## split
```sql
function split(varchar, varchar) returns array<varchar>
```


```sql
function split(varchar, varchar, bigint) returns array<varchar>
```


## split_part
```sql
function split_part(varchar, varchar, bigint) returns varchar
```


## split_to_map
```sql
function split_to_map(varchar, varchar, varchar) returns map<varchar, varchar>
```


## split_to_multimap
```sql
function split_to_multimap(varchar, varchar, varchar) returns map<varchar, array<varchar>>
```


## spooky_hash_v2_32
```sql
function spooky_hash_v2_32(bytea) returns bytea
```


## spooky_hash_v2_64
```sql
function spooky_hash_v2_64(bytea) returns bytea
```


## sqrt
```sql
function sqrt(double) returns double
```


## st_area
```sql
function st_area(varchar) returns double
```


```sql
function st_area(varchar) returns double
```


## st_asbinary
```sql
function st_asbinary(varchar) returns bytea
```


## st_astext
```sql
function st_astext(varchar) returns varchar
```


## st_boundary
```sql
function st_boundary(varchar) returns varchar
```


## st_buffer
```sql
function st_buffer(varchar, double) returns varchar
```


## st_centroid
```sql
function st_centroid(varchar) returns varchar
```


## st_contains
```sql
function st_contains(varchar, varchar) returns boolean
```


## st_convexhull
```sql
function st_convexhull(varchar) returns varchar
```


## st_coorddim
```sql
function st_coorddim(varchar) returns tinyint
```


## st_crosses
```sql
function st_crosses(varchar, varchar) returns boolean
```


## st_difference
```sql
function st_difference(varchar, varchar) returns varchar
```


## st_dimension
```sql
function st_dimension(varchar) returns tinyint
```


## st_disjoint
```sql
function st_disjoint(varchar, varchar) returns boolean
```


## st_endpoint
```sql
function st_endpoint(varchar) returns varchar
```


## st_envelope
```sql
function st_envelope(varchar) returns varchar
```


## st_envelopeaspts
```sql
function st_envelopeaspts(varchar) returns array<varchar>
```


## st_equals
```sql
function st_equals(varchar, varchar) returns boolean
```


## st_exteriorring
```sql
function st_exteriorring(varchar) returns varchar
```


## st_geometries
```sql
function st_geometries(varchar) returns array<varchar>
```


## st_geometryfromtext
```sql
function st_geometryfromtext(varchar) returns varchar
```


## st_geometryn
```sql
function st_geometryn(varchar, bigint) returns varchar
```


## st_geometrytype
```sql
function st_geometrytype(varchar) returns varchar
```


## st_geomfrombinary
```sql
function st_geomfrombinary(bytea) returns varchar
```


## st_interiorringn
```sql
function st_interiorringn(varchar, bigint) returns varchar
```


## st_interiorrings
```sql
function st_interiorrings(varchar) returns array<varchar>
```


## st_intersection
```sql
function st_intersection(varchar, varchar) returns varchar
```


## st_intersects
```sql
function st_intersects(varchar, varchar) returns boolean
```


## st_isclosed
```sql
function st_isclosed(varchar) returns boolean
```


## st_isempty
```sql
function st_isempty(varchar) returns boolean
```


## st_isring
```sql
function st_isring(varchar) returns boolean
```


## st_issimple
```sql
function st_issimple(varchar) returns boolean
```


## st_isvalid
```sql
function st_isvalid(varchar) returns boolean
```


## st_linefromtext
```sql
function st_linefromtext(varchar) returns varchar
```


## st_linestring
```sql
function st_linestring(array<varchar>) returns varchar
```


## st_multipoint
```sql
function st_multipoint(array<varchar>) returns varchar
```


## st_numgeometries
```sql
function st_numgeometries(varchar) returns int
```


## st_numpoints
```sql
function st_numpoints(varchar) returns bigint
```


## st_overlaps
```sql
function st_overlaps(varchar, varchar) returns boolean
```


## st_point
```sql
function st_point(double, double) returns varchar
```


## st_pointn
```sql
function st_pointn(varchar, bigint) returns varchar
```


## st_points
```sql
function st_points(varchar) returns array<varchar>
```


## st_polygon
```sql
function st_polygon(varchar) returns varchar
```


## st_relate
```sql
function st_relate(varchar, varchar, varchar) returns boolean
```


## st_startpoint
```sql
function st_startpoint(varchar) returns varchar
```


## st_symdifference
```sql
function st_symdifference(varchar, varchar) returns varchar
```


## st_touches
```sql
function st_touches(varchar, varchar) returns boolean
```


## st_union
```sql
function st_union(varchar, varchar) returns varchar
```


## st_within
```sql
function st_within(varchar, varchar) returns boolean
```


## st_x
```sql
function st_x(varchar) returns double
```


## st_y
```sql
function st_y(varchar) returns double
```


## starts_with
```sql
function starts_with(varchar, varchar) returns boolean
```


## stddev
```sql
function stddev(bigint) returns double
```


```sql
function stddev(double) returns double
```


## stddev_pop
```sql
function stddev_pop(bigint) returns double
```


```sql
function stddev_pop(double) returns double
```


## stddev_samp
```sql
function stddev_samp(bigint) returns double
```


```sql
function stddev_samp(double) returns double
```


## strpos
```sql
function strpos(varchar, varchar) returns bigint
```


```sql
function strpos(varchar, varchar, bigint) returns bigint
```


## substr
```sql
function substr(bytea, bigint) returns bytea
```


```sql
function substr(bytea, bigint, bigint) returns bytea
```


```sql
function substr(varchar, bigint) returns varchar
```


```sql
function substr(varchar, bigint, bigint) returns varchar
```


```sql
function substr(varchar, bigint) returns varchar
```


```sql
function substr(varchar, bigint, bigint) returns varchar
```


## tdigest_agg
```sql
function tdigest_agg(double) returns varchar
```


```sql
function tdigest_agg(double, double) returns varchar
```


## to_base
```sql
function to_base(bigint, bigint) returns varchar
```


## to_base32
```sql
function to_base32(bytea) returns varchar
```


## to_base64
```sql
function to_base64(bytea) returns varchar
```


## to_base64url
```sql
function to_base64url(bytea) returns varchar
```


## to_big_endian_32
```sql
function to_big_endian_32(bigint) returns bytea
```


## to_big_endian_64
```sql
function to_big_endian_64(bigint) returns bytea
```


## to_char
```sql
function to_char(timestamp(p), varchar) returns varchar
```


## to_encoded_polyline
```sql
function to_encoded_polyline(varchar) returns varchar
```


## to_geojson_geometry
```sql
function to_geojson_geometry(varchar) returns varchar
```


## to_geometry
```sql
function to_geometry(varchar) returns varchar
```


## to_hex
```sql
function to_hex(bytea) returns varchar
```


## to_ieee754_32
```sql
function to_ieee754_32(float) returns bytea
```


## to_ieee754_64
```sql
function to_ieee754_64(double) returns bytea
```


## to_iso8601
```sql
function to_iso8601(date) returns varchar
```


```sql
function to_iso8601(timestamp(p)) returns varchar
```


```sql
function to_iso8601(timestamp(p)) returns varchar
```


## to_milliseconds
```sql
function to_milliseconds(interval day to second) returns bigint
```


## to_spherical_geography
```sql
function to_spherical_geography(varchar) returns varchar
```


## to_utf8
```sql
function to_utf8(varchar) returns bytea
```


## transform
```sql
function transform(array(t), lambda <(t) -> u>) returns array(u)
```


## transform_keys
```sql
function transform_keys(map(k1, v), lambda <(k1, v) -> k2>) returns map(k2, v)
```


## transform_values
```sql
function transform_values(map(k, v1), lambda <(k, v1) -> v2>) returns map(k, v2)
```


## translate
```sql
function translate(varchar, varchar, varchar) returns varchar
```


## trim
```sql
function trim(varchar) returns varchar
```


```sql
function trim(varchar, varchar) returns varchar
```


```sql
function trim(varchar) returns varchar
```


```sql
function trim(varchar, varchar) returns varchar
```


## trim_array
```sql
function trim_array(array(e), bigint) returns array(e)
```


## truncate
```sql
function truncate(decimal(p, s), bigint) returns decimal(p, s)
```


```sql
function truncate(decimal(p, s)) returns decimal(rp, 0)
```


```sql
function truncate(double) returns double
```


```sql
function truncate(float) returns float
```


## try
```sql
function try(T) returns T
```


## typeof
```sql
function typeof(t) returns varchar
```


## upper
```sql
function upper(varchar) returns varchar
```


```sql
function upper(varchar) returns varchar
```


## url_decode
```sql
function url_decode(varchar) returns varchar
```


## url_encode
```sql
function url_encode(varchar) returns varchar
```


## url_extract_fragment
```sql
function url_extract_fragment(varchar) returns varchar
```


## url_extract_host
```sql
function url_extract_host(varchar) returns varchar
```


## url_extract_parameter
```sql
function url_extract_parameter(varchar, varchar) returns varchar
```


## url_extract_path
```sql
function url_extract_path(varchar) returns varchar
```


## url_extract_port
```sql
function url_extract_port(varchar) returns bigint
```


## url_extract_protocol
```sql
function url_extract_protocol(varchar) returns varchar
```


## url_extract_query
```sql
function url_extract_query(varchar) returns varchar
```


## uuid
```sql
function uuid() returns varchar
```


## value_at_quantile
```sql
function value_at_quantile(varchar, double) returns double
```


## values_at_quantiles
```sql
function values_at_quantiles(varchar, array<double>) returns array<double>
```


## var_pop
```sql
function var_pop(bigint) returns double
```


```sql
function var_pop(double) returns double
```


## var_samp
```sql
function var_samp(bigint) returns double
```


```sql
function var_samp(double) returns double
```


## variance
```sql
function variance(bigint) returns double
```


```sql
function variance(double) returns double
```


## week
```sql
function week(date) returns bigint
```


```sql
function week(timestamp(p)) returns bigint
```


```sql
function week(timestamp(p)) returns bigint
```


## week_of_year
```sql
function week_of_year(date) returns bigint
```


```sql
function week_of_year(timestamp(p)) returns bigint
```


```sql
function week_of_year(timestamp(p)) returns bigint
```


## width_bucket
```sql
function width_bucket(double, array<double>) returns bigint
```


```sql
function width_bucket(double, double, double, bigint) returns bigint
```


## wilson_interval_lower
```sql
function wilson_interval_lower(bigint, bigint, double) returns double
```


## wilson_interval_upper
```sql
function wilson_interval_upper(bigint, bigint, double) returns double
```


## word_stem
```sql
function word_stem(varchar) returns varchar
```


```sql
function word_stem(varchar, varchar) returns varchar
```


## xxhash64
```sql
function xxhash64(bytea) returns bytea
```


## year
```sql
function year(date) returns bigint
```


```sql
function year(interval day to second) returns bigint
```


```sql
function year(timestamp(p)) returns bigint
```


```sql
function year(timestamp(p)) returns bigint
```


## year_of_week
```sql
function year_of_week(date) returns bigint
```


```sql
function year_of_week(timestamp(p)) returns bigint
```


```sql
function year_of_week(timestamp(p)) returns bigint
```


## yow
```sql
function yow(date) returns bigint
```


```sql
function yow(timestamp(p)) returns bigint
```


```sql
function yow(timestamp(p)) returns bigint
```


## zip
```sql
function zip(array(t1), array(t2)) returns array(row(c0 t1, c1 t2))
```


```sql
function zip(array(t1), array(t2), array(t3)) returns array(row(c0 t1, c1 t2, c2 t3))
```


```sql
function zip(array(t1), array(t2), array(t3), array(t4)) returns array(row(c0 t1, c1 t2, c2 t3, c3 t4))
```


```sql
function zip(array(t1), array(t2), array(t3), array(t4), array(t5)) returns array(row(c0 t1, c1 t2, c2 t3, c3 t4, c4 t5))
```


## zip_with
```sql
function zip_with(array(t), array(u), lambda <(t, u) -> r>) returns array(r)
```



$sdf show --functions-markdown --dialect presto --section other --quiet

```
