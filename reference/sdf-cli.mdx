---
title: "SDF CLI Reference"
description: "This document contains the help content for the sdf command-line program."
---

# Command-Line Help for `sdf`

This document contains the help content for the `sdf` command-line program.

**Command Overview:**

* [`sdf`↴](#sdf)
* [`sdf new`↴](#sdf-new)
* [`sdf clean`↴](#sdf-clean)
* [`sdf compile`↴](#sdf-compile)
* [`sdf run`↴](#sdf-run)
* [`sdf test`↴](#sdf-test)
* [`sdf report`↴](#sdf-report)
* [`sdf build`↴](#sdf-build)
* [`sdf ai`↴](#sdf-ai)
* [`sdf ai classify`↴](#sdf-ai-classify)
* [`sdf lineage`↴](#sdf-lineage)
* [`sdf push`↴](#sdf-push)
* [`sdf pull`↴](#sdf-pull)
* [`sdf add`↴](#sdf-add)
* [`sdf system`↴](#sdf-system)
* [`sdf system update`↴](#sdf-system-update)
* [`sdf system uninstall`↴](#sdf-system-uninstall)
* [`sdf auth`↴](#sdf-auth)
* [`sdf auth login`↴](#sdf-auth-login)
* [`sdf auth logout`↴](#sdf-auth-logout)
* [`sdf auth status`↴](#sdf-auth-status)
* [`sdf auth aws`↴](#sdf-auth-aws)
* [`sdf auth openai`↴](#sdf-auth-openai)
* [`sdf man`↴](#sdf-man)
* [`sdf man cli`↴](#sdf-man-cli)
* [`sdf man functions`↴](#sdf-man-functions)
* [`sdf man definition-schema`↴](#sdf-man-definition-schema)
* [`sdf man deploy-schema`↴](#sdf-man-deploy-schema)
* [`sdf man information-schema`↴](#sdf-man-information-schema)
* [`sdf dbt`↴](#sdf-dbt)
* [`sdf dbt init`↴](#sdf-dbt-init)

## `sdf`

SDF's modular SQL

**Usage:** `sdf [OPTIONS]  COMMAND `

###### **Subcommands:**

* `new` — Create a new sdf workspace
* `clean` — Remove artifacts that sdf has generated in the past
* `compile` — Compile models
* `run` — Run models
* `test` — Test contracts for models
* `report` — Report stats for models
* `build` — Runs models, tests contracts, reports stats
* `ai` — Infer metadata like classifiers and add them optionally to the workspace
* `lineage` — Display lineage for a given column
* `push` — Push a local workspace to the SDF Service
* `pull` — Pull a remote workspace from the SDF Service
* `add` — Add new versioned files to the workspace
* `system` — System maintenance, install and update, reference docs
* `auth` — Authenticate client to services: SDF, AWS, OpenAI etc
* `man` — Display reference material, CLI, functions, schemas
* `dbt` — Initialize an sdf workspace from an existing dbt project

###### **Options:**

* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf new`

Create a new sdf workspace

**Usage:** `sdf new [OPTIONS] [PATH]`

###### **Arguments:**

* ` PATH ` — Create a new sdf workspace at  path 

###### **Options:**

* `--list-samples` — List all available samples

  Default value: `false`
* `--sample  SAMPLE ` — Create a workspace with the sample content at  path 
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf clean`

Remove artifacts that sdf has generated in the past

**Usage:** `sdf clean [OPTIONS]`

###### **Options:**

* `-p`, `--profile  PROFILE ` — Remove artifacts at  path  that sdf has created in the past [default: current workspace directory] Clean cache entries for this profile
* `--path  PATH `
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf compile`

Compile models

**Usage:** `sdf compile [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Compile only the given source dirs, files or tables and run code contracts [default: all models]

###### **Options:**

* `--no-code-contract` — Do NOT run code code contracts

  Default value: `false`
* `--no-propagate` — Do NOT perform propagate phase, short form of --stage resolve

  Default value: `false`
* `--infer-functions` — Infer function signature

  Default value: `false`
* `-p`, `--profile  PROFILE ` — Use this profile
* `-Q`, `--query  QUERY ` — Supply a .sql file or provide a sql snippet on the cmd line, e.g. 'select * from t'
* `--stage  STAGE ` — Run the following stages [default: all stages]

  Possible values: `parse`, `resolve`, `classify`, `execute`

* `--cache  CACHE ` — Controls cache use

  Default value: `read-write`

  Possible values: `read-write`, `write-only`, `read-only`, `none`

* `--no-cache` — Shorthand for cache policy none

  Default value: `false`
* `--save-assembly` — Saves assembly.sdf.yml and sdf.information_schema.* in sdfcache/profile/...

  Default value: `false`
* `--var  VAR ` — Supply var bindings as a yml file or provide them as string e.g. '(key: value)'
* `-n`, `--no-show` — Don't show the result of queries

  Default value: `false`
* `-s`, `--show` — Show the result of queries

  Default value: `false`
* `--display-mode  DISPLAY_MODE ` — Display messages

  Default value: `verbose`

  Possible values: `verbose`, `log-only`, `result-only`, `quiet`

* `--display-format  DISPLAY_FORMAT ` — Show tables in this format

  Default value: `table`

  Possible values: `table`, `csv`, `tsv`, `json`, `yml`

* `--limit  LIMIT ` — Limiting number of shown rows. Run with --limit 0 to remove limit

  Default value: `10`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf run`

Run models

**Usage:** `sdf run [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Run only the given source dirs, files or @tables [default: all models]

###### **Options:**

* `-p`, `--profile  PROFILE ` — Use this profile
* `-Q`, `--query  QUERY ` — Supply a .sql file or provide a sql snippet on the cmd line, e.g. 'select * from t'
* `--stage  STAGE ` — Run the following stages [default: all stages]

  Possible values: `parse`, `resolve`, `classify`, `execute`

* `--cache  CACHE ` — Controls cache use

  Default value: `read-write`

  Possible values: `read-write`, `write-only`, `read-only`, `none`

* `--no-cache` — Shorthand for cache policy none

  Default value: `false`
* `--save-assembly` — Saves assembly.sdf.yml and sdf.information_schema.* in sdfcache/profile/...

  Default value: `false`
* `--var  VAR ` — Supply var bindings as a yml file or provide them as string e.g. '(key: value)'
* `-d`, `--date  DATE ` — Run command for this  date , use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--from  FROM ` — Run command for all dates from  from  (inclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--to  TO ` — Run command for all dates to  to   (exclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format [default: now]
* `--dry-run` — Plan command but don't evaluate it

  Default value: `false`
* `-n`, `--no-show` — Don't show the result of queries

  Default value: `false`
* `-s`, `--show` — Show the result of queries

  Default value: `false`
* `--display-mode  DISPLAY_MODE ` — Display messages

  Default value: `verbose`

  Possible values: `verbose`, `log-only`, `result-only`, `quiet`

* `--display-format  DISPLAY_FORMAT ` — Show tables in this format

  Default value: `table`

  Possible values: `table`, `csv`, `tsv`, `json`, `yml`

* `--limit  LIMIT ` — Limiting number of shown rows. Run with --limit 0 to remove limit

  Default value: `10`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf test`

Test contracts for models

**Usage:** `sdf test [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Test only the given source dirs, files or @tables [default: all contracts]

###### **Options:**

* `--code` — Only run code code contracts

  Default value: `false`
* `--data` — Only run code code contracts

  Default value: `false`
* `-p`, `--profile  PROFILE ` — Use this profile
* `-Q`, `--query  QUERY ` — Supply a .sql file or provide a sql snippet on the cmd line, e.g. 'select * from t'
* `--stage  STAGE ` — Run the following stages [default: all stages]

  Possible values: `parse`, `resolve`, `classify`, `execute`

* `--cache  CACHE ` — Controls cache use

  Default value: `read-write`

  Possible values: `read-write`, `write-only`, `read-only`, `none`

* `--no-cache` — Shorthand for cache policy none

  Default value: `false`
* `--save-assembly` — Saves assembly.sdf.yml and sdf.information_schema.* in sdfcache/profile/...

  Default value: `false`
* `--var  VAR ` — Supply var bindings as a yml file or provide them as string e.g. '(key: value)'
* `-d`, `--date  DATE ` — Run command for this  date , use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--from  FROM ` — Run command for all dates from  from  (inclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--to  TO ` — Run command for all dates to  to   (exclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format [default: now]
* `--dry-run` — Plan command but don't evaluate it

  Default value: `false`
* `-n`, `--no-show` — Don't show the result of queries

  Default value: `false`
* `-s`, `--show` — Show the result of queries

  Default value: `false`
* `--display-mode  DISPLAY_MODE ` — Display messages

  Default value: `verbose`

  Possible values: `verbose`, `log-only`, `result-only`, `quiet`

* `--display-format  DISPLAY_FORMAT ` — Show tables in this format

  Default value: `table`

  Possible values: `table`, `csv`, `tsv`, `json`, `yml`

* `--limit  LIMIT ` — Limiting number of shown rows. Run with --limit 0 to remove limit

  Default value: `10`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf report`

Report stats for models

**Usage:** `sdf report [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Test only the given source dirs, files or @tables [default: all contracts]

###### **Options:**

* `--code` — Only run code code contracts

  Default value: `false`
* `--data` — Only run code code contracts

  Default value: `false`
* `-p`, `--profile  PROFILE ` — Use this profile
* `-Q`, `--query  QUERY ` — Supply a .sql file or provide a sql snippet on the cmd line, e.g. 'select * from t'
* `--stage  STAGE ` — Run the following stages [default: all stages]

  Possible values: `parse`, `resolve`, `classify`, `execute`

* `--cache  CACHE ` — Controls cache use

  Default value: `read-write`

  Possible values: `read-write`, `write-only`, `read-only`, `none`

* `--no-cache` — Shorthand for cache policy none

  Default value: `false`
* `--save-assembly` — Saves assembly.sdf.yml and sdf.information_schema.* in sdfcache/profile/...

  Default value: `false`
* `--var  VAR ` — Supply var bindings as a yml file or provide them as string e.g. '(key: value)'
* `-d`, `--date  DATE ` — Run command for this  date , use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--from  FROM ` — Run command for all dates from  from  (inclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--to  TO ` — Run command for all dates to  to   (exclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format [default: now]
* `--dry-run` — Plan command but don't evaluate it

  Default value: `false`
* `-n`, `--no-show` — Don't show the result of queries

  Default value: `false`
* `-s`, `--show` — Show the result of queries

  Default value: `false`
* `--display-mode  DISPLAY_MODE ` — Display messages

  Default value: `verbose`

  Possible values: `verbose`, `log-only`, `result-only`, `quiet`

* `--display-format  DISPLAY_FORMAT ` — Show tables in this format

  Default value: `table`

  Possible values: `table`, `csv`, `tsv`, `json`, `yml`

* `--limit  LIMIT ` — Limiting number of shown rows. Run with --limit 0 to remove limit

  Default value: `10`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf build`

Runs models, tests contracts, reports stats

**Usage:** `sdf build [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Build only the given source dirs, files or tables  [default: all tables]

###### **Options:**

* `-p`, `--profile  PROFILE ` — Use this profile
* `-Q`, `--query  QUERY ` — Supply a .sql file or provide a sql snippet on the cmd line, e.g. 'select * from t'
* `--stage  STAGE ` — Run the following stages [default: all stages]

  Possible values: `parse`, `resolve`, `classify`, `execute`

* `--cache  CACHE ` — Controls cache use

  Default value: `read-write`

  Possible values: `read-write`, `write-only`, `read-only`, `none`

* `--no-cache` — Shorthand for cache policy none

  Default value: `false`
* `--save-assembly` — Saves assembly.sdf.yml and sdf.information_schema.* in sdfcache/profile/...

  Default value: `false`
* `--var  VAR ` — Supply var bindings as a yml file or provide them as string e.g. '(key: value)'
* `-d`, `--date  DATE ` — Run command for this  date , use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--from  FROM ` — Run command for all dates from  from  (inclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format
* `--to  TO ` — Run command for all dates to  to   (exclusive), use YYYY-MM-DD or YYYY-MM-DDTHH:MM format [default: now]
* `--dry-run` — Plan command but don't evaluate it

  Default value: `false`
* `-n`, `--no-show` — Don't show the result of queries

  Default value: `false`
* `-s`, `--show` — Show the result of queries

  Default value: `false`
* `--display-mode  DISPLAY_MODE ` — Display messages

  Default value: `verbose`

  Possible values: `verbose`, `log-only`, `result-only`, `quiet`

* `--display-format  DISPLAY_FORMAT ` — Show tables in this format

  Default value: `table`

  Possible values: `table`, `csv`, `tsv`, `json`, `yml`

* `--limit  LIMIT ` — Limiting number of shown rows. Run with --limit 0 to remove limit

  Default value: `10`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf ai`

Infer metadata like classifiers and add them optionally to the workspace

**Usage:** `sdf ai  COMMAND `

###### **Subcommands:**

* `classify` — Auto classify columns



## `sdf ai classify`

Auto classify columns

**Usage:** `sdf ai classify [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Classify only the given source dirs, files or @tables [default: all root tables]

###### **Options:**

* `-p`, `--profile  PROFILE ` — Scope inference to using this profile
* `--save-location  SAVE_LOCATION ` — Add inferred classifiers to this include path [default: ./src]
* `--classifiers  CLASSIFIERS ` — Infer these classifiers [default: all classifiers]
* `--no-use-of-samples` — Do NOT pass samples to inference engine

  Default value: `false`
* `--no-use-of-data` — Do NOT pass at most 5 data values to inference engine

  Default value: `false`
* `--no-use-of-description` — Do NOT pass table/column descriptions to inference engine

  Default value: `false`
* `-c`, `--config  CONFIG ` — Supply a config yml file or provide config as yml string e.g. '(key: value)'
* `-y`, `--yes` — Answer yes to all prompts

  Default value: `false`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf lineage`

Display lineage for a given column

**Usage:** `sdf lineage [OPTIONS] [TARGETS]...`

###### **Arguments:**

* ` TARGETS ` — Target selection: compute the lineage for the given source dirs, files or @tables [default: all models]

###### **Options:**

* `--column  COLUMN ` — The column for which to compute lineage
* `--forward` — Compute downstream lineage instead of the default upstream lineage

  Default value: `false`
* `--show-scans` — Display scan dependencies in addition to copy and mod dependencies. Unset by default

  Default value: `false`
* `-p`, `--profile  PROFILE ` — Use this profile
* `-Q`, `--query  QUERY ` — Supply a .sql file or provide a sql snippet on the cmd line, e.g. 'select * from t'
* `--stage  STAGE ` — Run the following stages [default: all stages]

  Possible values: `parse`, `resolve`, `classify`, `execute`

* `--cache  CACHE ` — Controls cache use

  Default value: `read-write`

  Possible values: `read-write`, `write-only`, `read-only`, `none`

* `--no-cache` — Shorthand for cache policy none

  Default value: `false`
* `--save-assembly` — Saves assembly.sdf.yml and sdf.information_schema.* in sdfcache/profile/...

  Default value: `false`
* `--var  VAR ` — Supply var bindings as a yml file or provide them as string e.g. '(key: value)'
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf push`

Push a local workspace to the SDF Service

**Usage:** `sdf push [OPTIONS]`

###### **Options:**

* `--path  PATH ` — Push the workspace at  path  to the SDF Service [default: current workspace directory]
* `--delete` — Delete this workspace from the SDF Service

  Default value: `false`
* `-y`, `--yes` — Answer yes to all prompts

  Default value: `false`
* `-d`, `--dry-run` — No changes will be made to the console when this is set

  Default value: `false`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf pull`

Pull a remote workspace from the SDF Service

**Usage:** `sdf pull [OPTIONS]`

###### **Options:**

* `--name  NAME ` — Pull the workspace with this  name  from the SDF Service [default: name of current workspace]
* `--path  PATH ` — Create or override the workspace at  path   [default: current workspace directory]
* `-y`, `--yes` — Answer yes to all prompts

  Default value: `false`
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf add`

Add new versioned files to the workspace

**Usage:** `sdf add [OPTIONS] [INPUT_FILES]...`

###### **Arguments:**

* ` INPUT_FILES ` — Add these source dirs and/or files to the selected workspace, profile and includes-dir

###### **Options:**

* `--path  PATH ` — Add these files to the workspace at  path  [default: this workspace]
* `-p`, `--profile  PROFILE ` — Add the files using this profile [default: this workspace profile]
* `--includes-dir  INCLUDES_DIR ` — Add them to this include path [default: ./src]
* `--dialect  DIALECT ` — Dialect for all files [default: includes-dir dialect]

  Possible values: `snowflake`, `presto`, `bigquery`, `redshift`, `spark-lp`

* `--default-catalog  DEFAULT_CATALOG ` — Default catalog for all files [default: includes-dir catalog]
* `--default-schema  DEFAULT_SCHEMA ` — Default schema for all files [default: includes-dir schema]
* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf system`

System maintenance, install and update, reference docs

**Usage:** `sdf system  COMMAND `

###### **Subcommands:**

* `update` — Update sdf in place to the latest version
* `uninstall` — Uninstall sdf from the system



## `sdf system update`

Update sdf in place to the latest version

**Usage:** `sdf system update`

###### **Options:**

* `--version  VERSION ` — Update sdf to this  version   [default: latest version]



## `sdf system uninstall`

Uninstall sdf from the system

**Usage:** `sdf system uninstall`



## `sdf auth`

Authenticate client to services: SDF, AWS, OpenAI etc

**Usage:** `sdf auth  COMMAND `

###### **Subcommands:**

* `login` — Login to SDF Service
* `logout` — Log out of SDF Service
* `status` — Show status of credentials / tokens
* `aws` — Configure how to authenticate with AWS
* `openai` — Configure how to authenticate with OpenAI



## `sdf auth login`

Login to SDF Service

**Usage:** `sdf auth login [OPTIONS]`

###### **Options:**

* `--id-token  ID_TOKEN ` — Path to a file containing an OIDC identity token (a JWT)
* `--access-key  ACCESS_KEY ` — Access key for headless authentication
* `--secret-key  SECRET_KEY ` — Secret key for headless authentication
* `--credentials-dir  CREDENTIALS_DIR ` — Path to the file where credentials will be stored (default is platform specific)



## `sdf auth logout`

Log out of SDF Service

**Usage:** `sdf auth logout [OPTIONS]`

###### **Options:**

* `--aws` — Delete SDF's AWS Credential State

  Default value: `false`
* `--credentials-dir  CREDENTIALS_DIR ` — Path to the file where credentials are be stored (default is platform specific)



## `sdf auth status`

Show status of credentials / tokens

**Usage:** `sdf auth status [OPTIONS]`

###### **Options:**

* `--credentials-dir  CREDENTIALS_DIR ` — Path to the file where credentials will be stored (default is platform specific)



## `sdf auth aws`

Configure how to authenticate with AWS

**Usage:** `sdf auth aws [OPTIONS]`

###### **Options:**

* `--default-region  DEFAULT_REGION ` — AWS Region to use (default: us-east-1)

  Default value: `us-east-1`
* `--profile  PROFILE ` — AWS profile to use, as usually defined in ~/.aws/config or ~/.aws/credentials
* `--role-arn  ROLE_ARN ` — ARN of the role to assume
* `--external-id  EXTERNAL_ID ` — External ID to use when assuming the role
* `--use-web-identity` — Use web identity to authenticate

  Default value: `false`
* `--bucket-region-map  BUCKET_REGION_MAP ` — Mapping of bucket names to regions
* `--credentials-dir  CREDENTIALS_DIR ` — Path to the file where credentials will be stored (default is platform specific)



## `sdf auth openai`

Configure how to authenticate with OpenAI

**Usage:** `sdf auth openai [OPTIONS] --api-key  API_KEY `

###### **Options:**

* `--api-key  API_KEY ` — Mapping of bucket names to regions
* `--credentials-dir  CREDENTIALS_DIR ` — Path to the file where credentials will be stored (default is platform specific)



## `sdf man`

Display reference material, CLI, functions, schemas

**Usage:** `sdf man [OPTIONS]  COMMAND `

###### **Subcommands:**

* `cli` — Display SDF's command line interface [default only: markdown]
* `functions` — Display SDF's functions definitions [default: markdown, only markdown and yml]
* `definition-schema` — Display SDF's yml blocks as a json schema doc [default only: json]
* `deploy-schema` — Display SDF's deploy structure as a json schema doc [default only: json]
* `information-schema` — Display SDF's information schemas [default only: sql]

###### **Options:**

* `--man-format  MAN_FORMAT `

  Possible values: `sql`, `yml`, `markdown`, `json`




## `sdf man cli`

Display SDF's command line interface [default only: markdown]

**Usage:** `sdf man cli`



## `sdf man functions`

Display SDF's functions definitions [default: markdown, only markdown and yml]

**Usage:** `sdf man functions [OPTIONS]`

###### **Options:**

* `--dialect  DIALECT ` — Dialect for all functions

  Default value: `presto`

  Possible values: `snowflake`, `presto`, `bigquery`, `redshift`, `spark-lp`

* `--section  SECTION ` — Section to display [default: all]

  Default value: `all`



## `sdf man definition-schema`

Display SDF's yml blocks as a json schema doc [default only: json]

**Usage:** `sdf man definition-schema`



## `sdf man deploy-schema`

Display SDF's deploy structure as a json schema doc [default only: json]

**Usage:** `sdf man deploy-schema`



## `sdf man information-schema`

Display SDF's information schemas [default only: sql]

**Usage:** `sdf man information-schema`



## `sdf dbt`

Initialize an sdf workspace from an existing dbt project

**Usage:** `sdf dbt [OPTIONS]  COMMAND `

###### **Subcommands:**

* `init` — Initialize a sdf workspace from a dbt project -- best effort

###### **Options:**

* `-q`, `--quiet` — Do not print sdf log messages
* `--log-level  LOG_LEVEL ` — Set log level (by setting env var RUST_LOG)

  Possible values: `trace`, `debug`, `info`, `warn`, `error`

* `--cache-dir  CACHE_DIR ` — Set the  cache-dir  for this session
* `--credentials-dir  CREDENTIALS_DIR ` — Read the credentials from  credentials-dir 



## `sdf dbt init`

Initialize a sdf workspace from a dbt project -- best effort

**Usage:** `sdf dbt init [OPTIONS]`

###### **Options:**

* `--target  TARGET ` — Use this DBT target over the default target in profiles.yml
* `--profiles-dir  PROFILES_DIR ` — Use this DBT profile instead of the defaults at ~/.dbt/profile.yml -- (note dbt uses --profile_dir, this CLI uses --profile-dir)
* `-c`, `--config  CONFIG ` — Supply a config yml file or provide config as yml string e.g. '(key: value)'




