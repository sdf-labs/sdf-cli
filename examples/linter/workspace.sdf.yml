workspace:
  name: linter
  edition: "1.3"
  description: "An example workspace with linting and formatting issues"

  includes:
    - path: models
    - path: seeds
  
  defaults:
    dialect: snowflake
